const { coinspot } = require("ccxt")
const req = require("express/lib/request")
const router = require("./stocks")

router.get crypto user all coinspot
router.post req.date inputCryptoCode
router.post('/tradecrypto')
req.assetType